
const initialState = {
    id: null,
    name: '',
    pos: 0,
    fetching: false,
    fetched: false
};

const list = (state = initialState, action = {}) => {
    switch (action.type) {
    default:
        return state;
    }
};


export default list;
